
# Ender-Evac 控制中心

这是 Ender-Evac 项目的自动化跑路控制中心。

## 如何上传 Logo
若要使用你自己的 Logo：
1. 在项目根目录下（与 `package.json` 同级）创建一个名为 `public` 的文件夹（如果尚不存在）。
2. 将你的 logo 文件命名为 `icon.png` 并放入 `public` 文件夹中。
3. 确保路径为 `public/icon.png`。
4. 应用程序已配置为自动加载路径为 `/icon.png` 的文件。

## 开发指南
请查看 `src/app/page.tsx` 以开始修改界面。
